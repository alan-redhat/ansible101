---
- name: Get ping response
  hosts: all
    #  become: true
    #  become_user: aleberkn

  tasks:
    - name: ping
      ping:
      register: result

    - name: print result
      debug:
        msg: "{{ result }}"
~                           